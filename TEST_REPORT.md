# 测试报告

测试日期：2026 年 9 月 16 日
测试环境：Windows、Python 3.13.9、Pygame 2.6.1

## 测试方法

核心规则和存档使用 Python 标准库 `unittest` 自动测试；界面流程使用 Pygame 的 SDL 虚拟显示器测试，因此无需真正点击窗口也能重复验证。八张界面截图由程序自身渲染，并进行人工视觉检查。

执行命令：

```powershell
python -m unittest discover -v
python main.py --capture-screenshots docs/screenshots
```

## 作业指定测试结果

| 编号 | 测试内容 | 预期结果 | 实际结果 | 结论 |
| --- | --- | --- | --- | --- |
| T01 | 点击前方无阻挡的箭头 | 箭头飞出棋盘并消失 | 返回 `EXITED`，箭头数从 1 变为 0，飞出动画正常 | 通过 |
| T02 | 点击前方有阻挡的箭头 | 箭头不消失，失误次数减 1 | 返回 `BLOCKED`，箭头仍在，机会从 3 变为 2，并出现晃动/红线/文字 | 通过 |
| T03 | 点击位于边缘且朝向棋盘外的箭头 | 正常消失，不越界 | 上下左右四个边缘用例都返回 `EXITED`，无异常 | 通过 |
| T04 | 消除本关全部箭头 | 显示通关并进入下一关 | 按求解器序列点击后状态变为 `level_clear`，按钮可进入下一关 | 通过 |
| T05 | 失误次数耗尽 | 显示失败并允许重新开始 | 连续 3 次点击受阻箭头后状态变为 `failed`，出现重新挑战按钮 | 通过 |
| T06 | 游戏进行中重新开始 | 布局和失误次数恢复 | 重建关卡后箭头状态与初始状态相同，机会恢复到 3 | 通过 |

## 补充测试

| 测试项 | 实际结果 |
| --- | --- |
| 点击空格 | 不扣失误，不改变箭头数量 |
| 最近阻挡箭头判断 | 返回前进方向上的第一支箭头 |
| 关卡数量 | 共 5 关，满足至少 3 关要求 |
| 四方向覆盖 | 每个关卡都包含上、下、左、右 |
| 关卡可解性 | 深度优先搜索为 5 关都找到完整解序列 |
| 界面渲染 | 开始、游戏、通关、失败、全部通关五种状态均无异常 |
| Windows 字体兼容 | 绕过异常的 Pygame 系统字体索引，直接加载微软雅黑字体文件，中文渲染正常 |
| 撤销操作 | 成功消除后可恢复箭头、点击次数和计时 |
| AI 自动解题 | 按 DFS 解序列逐支播放动画并完成关卡，评价限制为 1 星 |
| 关卡解锁 | 第一关通过后第二关状态变为已解锁 |
| JSON 存档 | 保存和加载后星级、最佳分、最快时间一致 |
| 损坏存档 | 自动回退到新进度，程序继续运行 |
| 评分边界 | 无失误快速通关为 3 星；失误/提示会降低评价 |
| 博客与 README 截图 | 8 张 PNG 成功生成并人工检查，无文字重叠或字体缺失 |

## 最终自动测试输出摘要

```text
Ran 24 tests

OK
```

结论：基础玩法、状态流、边界处理、关卡数据和重新开始功能均达到作业要求。
