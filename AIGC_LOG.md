# AIGC 协作开发记录

记录日期：2026 年 9 月 15 日  
使用工具：OpenAI Codex

我向 Codex 提供了本次“一箭又一箭”小游戏的完整作业要求。以下内容按本次真实开发过程整理，不是项目完成后的虚构聊天记录。

## 记录一：核心规则与代码结构

| 项目 | 内容 |
| --- | --- |
| 我的要求 | 使用 Python/Pygame 实现单格箭头；点击后判断同一行或同一列前方是否有阻挡；代码需要方便测试。 |
| AI 完成 | 将项目拆分为 `core.py`、`levels.py` 和界面层；使用 `Direction` 枚举保存四个方向增量，用字典按坐标保存箭头，并生成 `blocker_for`、`can_exit` 和 `click` 方法。 |
| 实际效果 | 四个方向使用同一段循环判断，不需要分别复制四段代码；T01～T03 首轮测试通过。 |
| 人工判断/修改 | 检查了循环从相邻格开始、到边界停止的条件，确认不会把箭头自身当成阻挡，也不会访问棋盘外坐标。保留了界面与规则分离的设计。 |

代表性提示词摘要：

> 请先实现不依赖 Pygame 的棋盘逻辑，箭头用行、列和方向表示；路径检测要正确处理上下左右与边界，并为作业中的 T01～T06 编写测试。

## 记录二：关卡设计与可解性检查

| 项目 | 内容 |
| --- | --- |
| 我的要求 | 至少设计 3 个包含四种方向且能正常通关的原创关卡，最好能自动检查是否可解。 |
| AI 完成 | 设计了 5 个 7×7 关卡，并用深度优先搜索实现 `find_solution`；测试会逐关寻找完整消除顺序。 |
| 实际效果 | 第一轮测试发现“十字挑战”和“终极箭阵”不可解。原因是同一行或同一列存在两支相向箭头，双方一直互相阻挡。 |
| 人工判断/修改 | 根据求解结果定位死锁箭头，修改 3 支箭头方向后重新测试。最终 5 关都返回完整解序列，且每关均包含上、下、左、右。 |

第一轮真实测试结果：

```text
level='十字挑战' ... FAIL (solution is None)
level='终极箭阵' ... FAIL (solution is None)
```

修改后结果：

```text
test_every_level_has_a_solution ... ok
Ran 11 tests ... OK
```

这次过程说明 AI 生成的关卡不能直接假定正确，必须通过试玩或算法验证。

## 记录三：碰撞动画、界面与视觉验收

| 项目 | 内容 |
| --- | --- |
| 我的要求 | 制作开始、游戏、通关和失败界面；成功时要飞出，碰撞时要有明显反馈，并提供重新开始。 |
| AI 完成 | 使用 Pygame 基础图形绘制棋盘和箭头；实现飞出缓动、碰撞晃动、红色路径线、阻挡橙色高亮、提示文字，以及完整状态切换。还加入了提示按钮和计时。 |
| 实际效果 | 界面能够在 60 FPS 下运行，并能通过虚拟显示器生成四张真实截图。首次截图中，开始页装饰点与说明文字略有重叠；通关页的 Unicode 勾号因字体缺字显示成方框。 |
| 人工判断/修改 | 将装饰点改到对角方向；不再依赖字体绘制勾号，而是用两条白色线段画矢量勾。重新截图后显示正常。 |

代表性提示词摘要：

> 用统一配色实现 1000×760 的 Pygame 界面；碰撞必须同时有晃动、变色和文字提示；生成截图后做视觉检查，发现缺字或重叠要继续修改。

## 记录四：自动化测试与状态流程

| 项目 | 内容 |
| --- | --- |
| 我的要求 | 除规则单元测试外，还要验证真正的界面状态能进入通关页、失败页和重开状态。 |
| AI 完成 | 使用 SDL 虚拟显示器创建 `GameApp`，按求解序列逐个点击箭头；另外连续点击被阻挡箭头 3 次，并测试五种界面都能绘制。 |
| 实际效果 | 15 项测试全部通过，测试不需要人工点击窗口，适合提交前重复执行。 |
| 人工判断/修改 | 将每次飞行动画推进 0.6 秒后再点下一支箭头，保证测试过程与真实界面的“动画期间禁止重复点击”规则一致。 |

## 使用 AIGC 后的总结

Codex 最有帮助的部分是快速搭建代码结构、补充边界测试，以及用求解器发现肉眼不容易注意的关卡死锁。但 AI 的第一次输出仍然出现了不可解布局和字体兼容问题，因此不能只看代码是否能启动，还需要检查算法结果和真实渲染画面。我在本次开发中学到：游戏状态与界面分离后更容易测试；方向增量能减少重复代码；AI 结果必须通过测试和人工观察共同验证。
