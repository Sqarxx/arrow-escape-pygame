# “一箭又一箭”小游戏开发实践——使用 Python 与 AIGC 完成箭头解谜

| 项目内容 | 内容 |
| --- | --- |
| 这个作业属于哪个课程 | [2026 年春季软件工程与软件工程实践](https://edu.cnblogs.com/campus/fzu/2026-01SoftwareEngineeringandSoftwareEngineeringPractice/) |
| 这个作业要求在哪里 | [个人作业——AIGC 软件开发初体验](https://edu.cnblogs.com/campus/fzu/2026-01SoftwareEngineeringandSoftwareEngineeringPractice/homework/16718) |
| 这个作业的目标 | 使用 Python 和 AIGC 完成“一箭又一箭”小游戏 |
| 学号 | 102401514 |
| GitHub 仓库 | [Sqarxx/arrow-escape-pygame](https://github.com/Sqarxx/arrow-escape-pygame) |

## 1. 项目展示

我使用 Python 和 Pygame 完成了一款点击式箭头解谜小游戏。项目包含 5 个原创关卡，目标是判断箭头前方是否被阻挡，并用正确顺序清空棋盘。

### 开始界面

![开始界面](docs/screenshots/start.png)

### 游戏过程与碰撞反馈

下图中，红色箭头向下飞行时被橙色光圈中的箭头阻挡。程序用红色连线、箭头晃动、变色和底部文字同时提示碰撞，并扣除一次失误机会。

![游戏过程](docs/screenshots/game.png)

### 通关与失败界面

| 通关 | 失败 |
| --- | --- |
| ![通关](docs/screenshots/success.png) | ![失败](docs/screenshots/failure.png) |

## 2. 项目介绍

游戏规则是：每个箭头占据 7×7 网格中的一格，方向只有上、下、左、右。点击箭头后，程序沿它的朝向逐格检查。如果到棋盘边界之间没有其他箭头，它会播放飞出动画并消失；如果前方有箭头，它会晃动并显示碰撞反馈，同时减少一次失误机会。清空所有箭头后通过本关，机会耗尽则失败。

界面采用深蓝、亮蓝和青色作为主色。游戏页左侧是棋盘，右侧显示剩余箭头、失误机会、本关用时，以及重新开始、提示一步、返回首页三个按钮。除了作业要求，我还实现了计时和提示功能：按 `H` 或点击“提示一步”，程序会用求解器找出一支安全箭头并显示蓝色光圈。

## 3. 实现思路

### 3.1 数据表示与代码结构

我把规则层和界面层分开：

- `core.py`：方向、箭头、棋盘、点击结果和求解器；
- `levels.py`：5 个原创关卡数据；
- `app.py`：Pygame 绘图、按钮、动画和界面状态；
- `main.py`：启动入口和截图命令。

方向使用枚举保存行列增量：

```python
class Direction(Enum):
    UP = (-1, 0)
    DOWN = (1, 0)
    LEFT = (0, -1)
    RIGHT = (0, 1)
```

箭头用不可变数据类表示：

```python
@dataclass(frozen=True, slots=True)
class Arrow:
    row: int
    col: int
    direction: Direction
```

棋盘用字典保存箭头，键是 `(row, col)`。这样可以用接近 O(1) 的时间判断某个格子是否有箭头。

### 3.2 路径检测

路径检测是本项目的核心。程序先取得方向增量 `(dr, dc)`，从相邻格开始向前移动。如果在边界内找到箭头，就立即返回这支阻挡箭头；如果循环走出棋盘仍未找到，说明路径畅通。

```python
dr, dc = arrow.direction.delta
row, col = arrow.row + dr, arrow.col + dc
while self.in_bounds(row, col):
    blocker = self.arrows.get((row, col))
    if blocker is not None:
        return blocker
    row += dr
    col += dc
return None
```

这个写法的优点是四种方向共用同一段代码，只靠增量不同来控制移动，也自然处理了边缘箭头，不需要写四组容易出错的 `if`。

### 3.3 动画与状态管理

程序使用字符串区分 `start`、`playing`、`level_clear`、`failed` 和 `complete` 五种状态。每帧根据当前状态绘制对应界面。

成功点击时，逻辑层先移除箭头，界面层保存它的起点、终点和动画时间，用三次缓出曲线计算当前位置，让箭头先快后慢地飞出。碰撞时，使用正弦函数计算左右偏移产生晃动，同时把点击箭头变红、阻挡箭头加橙色光圈，并在两者之间绘制红线。

### 3.4 关卡可解性

仅凭肉眼设计关卡容易产生相向箭头死锁。我实现了一个深度优先搜索求解器：对当前所有“可以飞出”的箭头逐个尝试，递归搜索删除后的状态，并用缓存避免重复计算。测试程序要求每关都能返回与箭头数量相同的完整点击序列。

这不但验证关卡可通关，也被“提示一步”功能复用。

## 4. AIGC 使用过程

本项目在 2026 年 9 月 15 日使用 OpenAI Codex 辅助开发，下面是三次有代表性的真实过程。

| 子任务 | AIGC 工具 | AI 实现或提供了什么 | 实际效果 | 人工判断与修改 |
| --- | --- | --- | --- | --- |
| 路径检测与测试 | Codex | 设计方向枚举、棋盘字典和统一的逐格检测循环，并为边缘情况生成测试 | T01～T03 首轮通过，四方向不需要重复代码 | 检查起点和边界条件，确认不会把自身算作阻挡 |
| 关卡设计 | Codex | 生成 5 个关卡并实现 DFS 求解器 | 首轮发现第 4、5 关无解，测试返回 `None` | 分析出相向箭头死锁，修改 3 支箭头方向后全部可解 |
| 界面与碰撞动画 | Codex | 生成飞出、晃动、变色、连线和提示文字，并输出真实截图 | 功能可用，但首次截图有装饰重叠和勾号缺字 | 调整装饰位置，并将字体勾号改为 Pygame 线段绘制 |
| 状态流程测试 | Codex | 使用 SDL 虚拟显示器模拟通关、连续失误、重开和五种界面绘制 | 包括字体兼容检查在内的 16 项测试全部通过 | 调整测试中的动画推进时间，使其符合实际操作节奏 |

更完整的输入摘要、首轮失败结果与修改记录保存在仓库的 `AIGC_LOG.md`。

## 5. 测试结果

我使用 `python -m unittest discover -v` 执行自动测试，并用程序生成截图进行人工视觉检查。

| 编号 | 测试内容 | 预期结果 | 实际结果 | 是否通过 |
| --- | --- | --- | --- | --- |
| T01 | 点击前方无阻挡的箭头 | 飞出并消失 | 返回 `EXITED`，数量减 1，动画正常 | 是 |
| T02 | 点击前方有阻挡的箭头 | 不消失，失误减 1 | 返回 `BLOCKED`，箭头保留，机会减 1 | 是 |
| T03 | 点击边缘且朝外的箭头 | 正常消失，无越界 | 四个边缘方向均无异常 | 是 |
| T04 | 消除本关全部箭头 | 显示通关并可进下一关 | 状态进入 `level_clear`，按钮正常 | 是 |
| T05 | 失误次数耗尽 | 显示失败并允许重开 | 状态进入 `failed`，重试按钮正常 | 是 |
| T06 | 游戏中重新开始 | 布局与机会恢复 | 状态键和初始值完全一致 | 是 |

最终结果为 16 项全部通过；5 个关卡均找到完整解序列。详细测试记录见 `TEST_REPORT.md`。

## 6. PSP 表格

下面的实际耗时包含需求理解、阅读代码、亲自试玩和撰写博客。由于 AIGC 并行完成了部分样板代码，界面编码耗时比最初预计少，但关卡验证与视觉调整耗时比预想多。

| 任务 | 预估耗时（小时） | 实际耗时（小时） | 差异（小时） |
| --- | ---: | ---: | ---: |
| 需求分析与游戏设计 | 0.8 | 0.7 | -0.1 |
| Python 与图形库学习 | 1.0 | 0.6 | -0.4 |
| 游戏界面实现 | 2.5 | 1.8 | -0.7 |
| 路径与碰撞逻辑实现 | 1.5 | 1.2 | -0.3 |
| 关卡设计 | 1.0 | 1.3 | +0.3 |
| AIGC 辅助开发 | 0.8 | 0.8 | 0.0 |
| 测试与修改 | 1.2 | 1.5 | +0.3 |
| README 与博客撰写 | 1.2 | 1.1 | -0.1 |
| **合计** | **10.0** | **9.0** | **-1.0** |

> 提交前我会根据自己的最终学习、试玩和整理时间再次校准“实际耗时”，保证 PSP 记录与个人开发过程一致。

## 7. 心得体会

这次作业让我体会到，小游戏“规则简单”并不等于“实现没有细节”。路径检测本身只有一个循环，但还要考虑起点、边界、空格点击、动画期间重复操作、最后一支箭头消失后的状态切换等问题。把规则写成不依赖 Pygame 的类以后，这些情况都能通过单元测试快速重复检查。

AIGC 提高了搭建代码框架和编写测试的速度，也能给出较完整的界面方案，但它的输出不能直接当成最终答案。最典型的是 AI 首次生成的两个关卡存在死锁；如果只看界面能运行，很可能到提交后才发现无法通关。加入求解器后，错误被自动测试直接暴露。截图检查又发现了字体缺字，这说明算法测试和视觉测试缺一不可。

我的主要收获有三点：第一，用方向增量可以让四个方向共享一套逻辑；第二，游戏状态和图形界面分离能显著降低测试难度；第三，使用 AI 时最重要的能力不是复制代码，而是明确需求、验证结果、定位问题，并能解释为什么这样修改。后续如果继续扩展，我希望增加撤销功能、关卡选择和由求解序列反向生成随机可解关卡。
