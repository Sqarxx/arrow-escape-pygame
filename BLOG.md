# Python + AIGC 开发“一箭又一箭”小游戏实践

| 项目内容 | 内容 |
| --- | --- |
| 这个作业属于哪个课程 | [2026 年春季软件工程与软件工程实践](https://edu.cnblogs.com/campus/fzu/2026-01SoftwareEngineeringandSoftwareEngineeringPractice/) |
| 这个作业要求在哪里 | [个人作业——AIGC 软件开发初体验](https://edu.cnblogs.com/campus/fzu/2026-01SoftwareEngineeringandSoftwareEngineeringPractice/homework/16718) |
| 这个作业的目标 | 使用 Python 和 AIGC 完成“一箭又一箭”小游戏 |
| 学号 | 102401514 |
| GitHub 仓库 | [Sqarxx/arrow-escape-pygame](https://github.com/Sqarxx/arrow-escape-pygame) |

## 一、项目展示

本项目使用 Python 3.13 和 Pygame 2.6.1 开发，是一款单格箭头点击解谜游戏。玩家需要观察箭头方向以及同一行、同一列上的阻挡关系，按照正确顺序将所有箭头移出棋盘。

项目包含 5 个原创关卡，完成了开始、关卡选择、游戏、通关、失败和全部通关界面。除了基础要求，还实现了计时、星级、得分、最佳成绩、自动存档、撤销、提示和 AI 自动解题。

### 1. 开始界面

开始页使用浅青色箭头纹理、描边标题和原创表情箭头吉祥物。所有图形均通过 Pygame 绘制，没有使用原商业游戏素材。

![开始界面](docs/screenshots/start.png)

### 2. 关卡选择界面

关卡会按顺序解锁。界面显示每关获得的星级和最佳分数，关闭游戏后再次启动仍能恢复进度。

![关卡选择界面](docs/screenshots/levels.png)

### 3. 游戏过程与碰撞反馈

游戏页顶部显示当前关卡、失误机会、计时和剩余箭头；中间是 7×7 棋盘；底部提供重新开始、撤销、提示、AI 解题和返回首页按钮。

下图中，被点击的红色箭头前方存在阻挡。程序同时使用晃动、淡红变色、红色路径线、橙色阻挡光圈和文字提示反馈碰撞。

![游戏过程与碰撞反馈](docs/screenshots/game.png)

### 4. 通关与失败界面

通关时显示星级、本局得分、最佳分数、点击次数和用时；失误次数耗尽时进入失败页，并可重新挑战。

| 通关界面 | 失败界面 |
| --- | --- |
| ![通关界面](docs/screenshots/success.png) | ![失败界面](docs/screenshots/failure.png) |

## 二、需求分析与功能设计

### 1. 基础玩法

每支箭头占用棋盘中的一个格子，方向只有上、下、左、右四种。点击箭头后，从相邻格开始沿箭头方向检查：

- 如果箭头与棋盘边界之间没有其他箭头，箭头飞出并消失；
- 如果路径上存在其他箭头，箭头不能消失，并扣除一次失误机会；
- 清空所有箭头后通过当前关卡；
- 失误机会耗尽后挑战失败；
- 玩家可以重新开始当前关卡。

### 2. 功能完成情况

| 类别 | 已实现功能 |
| --- | --- |
| 基础界面 | 开始、游戏、通关、失败、全部通关界面 |
| 核心规则 | 四方向箭头、鼠标点击、路径检测、边界处理 |
| 反馈机制 | 飞出动画、碰撞晃动、变色、路径线和文字提示 |
| 关卡流程 | 5 个原创可解关卡、切换、失败重试、重新开始 |
| 状态显示 | 当前关卡、剩余箭头、失误机会、本关用时 |
| 附加功能 | 关卡选择、星级、得分、存档、撤销、提示、AI 自动解题 |
| 工程质量 | 规则与界面分离、24 项自动测试、Git 分阶段提交 |

### 3. 操作方式

| 操作 | 功能 |
| --- | --- |
| 鼠标左键 | 点击箭头或界面按钮 |
| `R` | 重新开始当前关卡 |
| `H` | 提示一个当前可安全飞出的箭头 |
| `U` | 撤销上一步操作 |
| `A` | 开始或停止 AI 自动解题演示 |
| `Esc` | 返回开始界面；在开始界面退出游戏 |
| `Enter` / `Space` | 开始游戏或在结果页继续 |

## 三、程序结构与实现思路

### 1. 项目结构

```text
arrow-escape-pygame/
├── arrow_game/
│   ├── app.py          # Pygame 界面、事件、动画和状态切换
│   ├── core.py         # 箭头、棋盘、路径检测和 DFS 求解器
│   ├── levels.py       # 5 个原创关卡
│   └── progress.py     # 星级、得分、进度和 JSON 存档
├── tests/
│   ├── test_game_logic.py
│   ├── test_app_flow.py
│   └── test_progress.py
├── docs/screenshots/   # 程序生成的界面截图
├── main.py             # 程序入口
├── requirements.txt    # Pygame 依赖
└── README.md
```

我将规则层与界面层分开。`core.py` 不依赖 Pygame，因此路径判断和关卡数据可以直接使用 `unittest` 测试；`app.py` 只负责把规则结果表现为界面和动画。

### 2. 箭头与方向表示

方向枚举同时保存行、列增量：

```python
class Direction(Enum):
    UP = (-1, 0)
    DOWN = (1, 0)
    LEFT = (0, -1)
    RIGHT = (0, 1)

    @property
    def delta(self) -> tuple[int, int]:
        return self.value
```

箭头用不可变数据类表示：

```python
@dataclass(frozen=True, slots=True)
class Arrow:
    row: int
    col: int
    direction: Direction
```

棋盘使用字典保存箭头，字典的键是 `(row, col)`，值是 `Arrow`。因此判断某个格子是否被占用不需要遍历整个列表。

### 3. 核心路径检测

路径检测是本项目最关键的算法。程序先找到被点击箭头，然后取得方向增量 `(dr, dc)`，从箭头前方的相邻格开始逐格移动：

```python
def blocker_for(self, position):
    arrow = self.arrows.get(position)
    if arrow is None:
        return None

    dr, dc = arrow.direction.delta
    row, col = arrow.row + dr, arrow.col + dc
    while self.in_bounds(row, col):
        blocker = self.arrows.get((row, col))
        if blocker is not None:
            return blocker
        row += dr
        col += dc
    return None
```

如果循环在棋盘范围内发现箭头，就返回最近的阻挡箭头；如果走出边界仍未找到，说明路径畅通。四个方向共用同一段代码，只需要改变方向增量，减少了重复判断和边界错误。

### 4. 点击处理与游戏状态

棋盘点击有三种结果：

- `EMPTY`：点击空格，不改变任何状态；
- `EXITED`：路径畅通，删除箭头；
- `BLOCKED`：前方有阻挡，保留箭头并减少失误机会。

界面使用 `start`、`level_select`、`playing`、`level_clear`、`failed` 和 `complete` 表示不同页面。每帧根据当前状态绘制对应界面，避免把所有按钮和逻辑放在一个函数中。

### 5. 飞出与碰撞动画

箭头成功飞出时，界面层保存起点、终点、持续时间和已经播放的时间，并用三次缓出曲线计算当前位置：

```python
progress = animation.progress
eased = 1 - (1 - progress) ** 3
position = animation.start.lerp(animation.end, eased)
```

碰撞时使用正弦函数产生左右晃动，并高亮阻挡箭头。动画播放期间暂时不接受新的棋盘点击，防止连续点击造成状态不同步。

### 6. 关卡设计与自动求解

关卡数据保存在 `levels.py`。每关包含棋盘大小、允许失误次数和箭头坐标。为了避免关卡出现无法消除的死锁，我实现了深度优先搜索：

1. 找出当前所有可以飞出棋盘的箭头；
2. 尝试删除其中一支箭头；
3. 递归求解剩余状态；
4. 如果某条分支能够清空棋盘，就返回完整点击顺序；
5. 使用缓存避免重复搜索相同棋盘状态。

测试程序会逐关调用求解器，并要求返回序列长度与关卡箭头数量相同。现有 5 关全部通过验证。提示功能使用求解序列的第一步；AI 自动解题则按时间间隔依次执行整个序列。

## 四、附加功能实现

### 1. 撤销上一步

每次有效点击前，程序保存一份轻量快照，内容包括棋盘副本、点击次数、提示次数和本关时间。撤销时取出最近快照，恢复箭头布局和失误机会，同时停止正在运行的 AI 自动解题。

这种方式既可以撤销成功消除，也可以撤销一次碰撞失误。重新开始或切换关卡时会清空快照，避免跨关卡恢复错误状态。

### 2. 星级和得分

星级综合考虑失误、用时、提示次数和是否使用 AI：

- 在目标时间内、无失误且未使用提示：3 星；
- 最多一次失误、最多一次提示，并在放宽时间内完成：2 星；
- 其他情况：1 星；
- 使用 AI 自动解题：最多获得 1 星。

得分从基础分中扣除耗时、失误、提示和 AI 惩罚，最低保留 100 分。这样既能展示扩展功能，又不会让 AI 自动完成获得最高评价。

### 3. JSON 自动存档

通关后，程序记录：

- 已解锁的最高关卡；
- 每关最高星级；
- 每关最佳分数；
- 每关最快时间。

存档文件为项目根目录下的 `save_data.json`。它已经加入 `.gitignore`，不会把个人游玩记录提交到 GitHub。读取存档时会检查字段类型和数值范围；如果文件损坏，则安全回退到新进度，避免游戏无法启动。

### 4. 原创界面绘制

界面参考了同类休闲游戏常见的版式结构，但没有复制商业素材：

- 封面的箭头纹理由程序循环绘制；
- 表情箭头吉祥物由多边形、圆形、线段和圆弧组成；
- 爱心、计时图标、星星和按钮全部由 Pygame 实时绘制；
- 游戏仍采用作业规定的单格箭头，没有改成其他游戏的折线路径玩法。

## 五、AIGC 使用过程

本项目在 2026 年 9 月 15～16 日使用 OpenAI Codex 辅助需求拆分、代码实现、调试、测试和界面设计。下面记录本次开发中具有代表性的真实过程。

| 子任务 | 使用工具 | 我提出的要求 | AI 提供的内容 | 实际效果与人工修改 |
| --- | --- | --- | --- | --- |
| 需求拆分与路径检测 | Codex | 将图形界面与核心规则分离，实现四方向路径检测并覆盖边界 | 生成 `Board`、`Direction`、`Arrow` 结构和 T01～T06 测试 | 统一增量循环正确处理四个方向；检查确认从相邻格开始，不会把自己当作阻挡 |
| 关卡设计与求解器 | Codex | 设计至少 3 个原创可解关卡，并自动检查 | 生成 5 个关卡和 DFS 求解器 | 首轮发现第 4、5 关无解；分析出相向箭头死锁，修改 3 支箭头方向后全部可解 |
| 界面与碰撞动画 | Codex | 完成开始、游戏、结果页以及飞出和碰撞反馈 | 实现 Pygame 绘图、飞出缓动、碰撞晃动、变色和提示 | 首次截图出现装饰重叠和勾号缺字；调整位置，并用线段绘制矢量勾号 |
| Windows 字体兼容 | Codex | 根据实际运行报错修复 `SysFont` 启动失败 | 定位到 Pygame 系统字体索引异常，改为直接加载 Windows 中文字体文件 | 在 Python 3.13/Pygame 2.6.1 环境重新验证，中文正常显示，并加入字体回归测试 |
| 附加功能 | Codex | 加入关卡选择、星级、保存进度、撤销和 AI 自动解题 | 新建 `progress.py`，实现快照、评分、JSON 存档与自动演示 | 将 AI 通关限制为 1 星；存档加入 `.gitignore`；新增损坏存档和状态恢复测试 |
| 视觉重构 | Codex | 改为浅青和青绿色风格，并参考箭头游戏常见封面/HUD版式 | 重构封面、吉祥物、顶部 HUD、中央棋盘和底部工具栏 | 保留单格规则；不导入参考图素材；生成截图检查文字、颜色和按钮区域 |

### 一次典型的 AI 输出验证过程

AI 最初生成关卡后，程序虽然能够启动，但自动测试出现：

```text
level='十字挑战' ... FAIL (solution is None)
level='终极箭阵' ... FAIL (solution is None)
```

检查后发现，同一行或同一列中的两支箭头相向而行，会永久互相阻挡。我修改了箭头方向，再次执行测试：

```text
test_every_level_has_a_solution ... ok
```

这个过程说明，AI 生成的内容不能只看是否能够运行，还要通过算法和实际画面验证。

完整 AIGC 过程记录保存在仓库的 `AIGC_LOG.md`。

## 六、测试过程与结果

### 1. 测试环境

- 操作系统：Windows
- Python：3.13.9
- Pygame：2.6.1
- 测试框架：Python 标准库 `unittest`

运行命令：

```powershell
python -m unittest discover -v
```

### 2. 作业要求测试

| 编号 | 测试内容 | 预期结果 | 实际结果 | 结论 |
| --- | --- | --- | --- | --- |
| T01 | 点击前方无阻挡的箭头 | 箭头飞出并消失 | 返回 `EXITED`，箭头数减少并播放动画 | 通过 |
| T02 | 点击前方有阻挡的箭头 | 不消失，失误次数减 1 | 返回 `BLOCKED`，箭头保留并显示碰撞反馈 | 通过 |
| T03 | 点击边缘且朝向棋盘外的箭头 | 正常消失，无越界错误 | 上、下、左、右四种边缘情况均无异常 | 通过 |
| T04 | 消除本关全部箭头 | 显示通关并进入下一关 | 进入 `level_clear`，下一关按钮可用 | 通过 |
| T05 | 失误次数耗尽 | 显示失败并允许重新开始 | 进入 `failed`，重新挑战按钮可用 | 通过 |
| T06 | 游戏进行中重新开始 | 布局和失误次数恢复 | 棋盘状态与初始状态一致 | 通过 |

### 3. 扩展与回归测试

| 编号 | 测试内容 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| T07 | 点击空格 | 不扣失误，不改变箭头数量 | 通过 |
| T08 | 最近阻挡箭头判断 | 返回前进方向上的第一支箭头 | 通过 |
| T09 | 所有关卡可解性 | 5 关均返回完整解序列 | 通过 |
| T10 | 撤销一步 | 恢复箭头、失误、点击次数和计时 | 通过 |
| T11 | AI 自动解题 | 逐支播放动画并完成关卡，评价为 1 星 | 通过 |
| T12 | 通关解锁 | 下一关变为可选择状态 | 通过 |
| T13 | JSON 保存与加载 | 星级、最高分和最快时间正确恢复 | 通过 |
| T14 | 损坏存档 | 安全回退到新进度，不抛出异常 | 通过 |
| T15 | 评分边界 | 3 星、2 星、AI 1 星规则正确 | 通过 |
| T16 | Windows 中文字体 | 字体成功加载并能渲染中文 | 通过 |
| T17 | 全部界面绘制 | 六种界面均可在虚拟显示器中绘制 | 通过 |

最终自动化测试结果：

```text
Ran 24 tests

OK
```

程序还提供截图命令，用真实绘制结果检查布局：

```powershell
python main.py --capture-screenshots docs/screenshots
```

五张截图均为 1000×760，经过人工检查，没有出现文字重叠、字体缺失或按钮越界。

## 七、Git 与项目提交

开发过程中按照功能分阶段提交，而不是最后一次性上传。主要 Commit 包括：

```text
feat: 实现路径检测和可解关卡数据
feat: 完成游戏界面、动画和状态流程
fix: 兼容Windows系统字体加载
feat: 增加关卡进度、撤销和AI自动解题
style: 改为青绿色浅色极简主题
style: 重构封面与游戏HUD布局
docs: 更新附加功能说明和界面截图
```

仓库中没有密码、Cookie、API Key 或 Token。虚拟环境、缓存、临时文件和个人存档均通过 `.gitignore` 排除。

## 八、PSP 表格

| 任务 | 预估耗时（小时） | 实际耗时（小时） | 差异（小时） |
| --- | ---: | ---: | ---: |
| 需求分析与游戏设计 | 0.8 | 0.7 | -0.1 |
| Python 与 Pygame 学习 | 1.0 | 0.8 | -0.2 |
| 路径与碰撞逻辑实现 | 1.5 | 1.2 | -0.3 |
| 基础游戏界面实现 | 2.0 | 3.0 | +1.0 |
| 关卡设计与可解性验证 | 1.0 | 1.2 | +0.2 |
| 扩展功能与存档实现 | 1.5 | 2.0 | +0.5 |
| AIGC 辅助开发与结果判断 | 0.8 | 1.0 | +0.2 |
| 测试、调试与兼容性修改 | 1.2 | 1.8 | +0.6 |
| README、博客与截图整理 | 1.2 | 1.5 | +0.3 |
| **合计** | **11.0** | **13.2** | **+2.2** |

实际耗时超过预估的主要原因是界面进行了多轮视觉调整，同时增加了存档、撤销、AI 自动解题和更多回归测试。AIGC 减少了样板代码编写时间，但验证和修改 AI 输出仍然需要投入时间。

## 九、心得体会

这次作业让我认识到，小游戏的规则虽然简单，但实现时仍然包含数据结构、坐标计算、状态切换、动画、文件读写和测试等多个方面。路径检测代码本身不长，真正容易出错的是边界情况、最后一支箭头消失后的流程、动画期间的重复操作，以及重新开始后状态是否完全恢复。

将规则层和 Pygame 界面分离是本次开发中很重要的决定。规则层可以在不打开窗口的情况下测试，界面层只负责显示结果。加入自动求解器后，不仅可以验证关卡是否可解，还能复用到提示和 AI 演示功能，说明一个清晰的算法模块可以同时服务多个功能。

AIGC 的主要价值是帮助我快速建立结构、补充测试和尝试界面方案，但它不能替代判断。第一次生成的关卡存在死锁，第一次字体实现也在实际 Windows 环境中报错；如果没有自动测试、真实运行和截图检查，这些问题很容易被忽略。经过这次实践，我认为正确使用 AIGC 的关键是：把需求描述清楚、理解关键代码、保留开发记录，并使用测试验证每次修改。

最终项目完成了全部基础要求，并在基础稳定的前提下实现了关卡选择、星级、得分、存档、撤销和 AI 自动解题。我也进一步熟悉了 Pygame 事件循环、二维坐标处理、深度优先搜索、JSON 持久化和 Git 分阶段提交。
